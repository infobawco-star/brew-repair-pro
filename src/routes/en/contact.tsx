import { createFileRoute } from "@tanstack/react-router";
import { useState, type FormEvent } from "react";
import { CheckCircle2, Mail, MapPin, MessageCircle, Phone } from "lucide-react";
import { PageHero, PageShell } from "../../components/site/PageShell";
import { CONTACT } from "../../lib/contact.config";
export const Route=createFileRoute("/en/contact")({head:()=>({meta:[{title:"Request Café Equipment Service — FixBar"},{name:"description",content:"Request espresso machine or grinder maintenance from FixBar through WhatsApp."},{property:"og:title",content:"Request Café Equipment Service — FixBar"},{property:"og:description",content:"Tell us about your equipment and send your request directly by WhatsApp."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Contact});
const inputCls="w-full rounded-lg border border-input bg-background/70 px-4 py-3 text-sm text-foreground placeholder:text-muted-foreground/60 outline-hidden transition-colors focus:border-primary focus:ring-2 focus:ring-primary/30";
function Contact(){const[sent,setSent]=useState(false);const[waUrl,setWaUrl]=useState("");function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const line=(key:string,label:string)=>`${label}: ${String(f.get(key)??"")}`;const text=["New service request — FixBar",line("name","Name"),line("phone","Phone"),line("cafe","Café"),line("city","City"),line("device","Equipment"),line("service","Service"),line("details","Details")].join("\n");const url=`https://api.whatsapp.com/send/?phone=${CONTACT.phone}&text=${encodeURIComponent(text)}`;setWaUrl(url);setSent(true);window.open(url,"_blank","noopener,noreferrer")}return <PageShell locale="en"><PageHero locale="en" eyebrow="REQUEST SERVICE" title="Tell us about your equipment. We’ll handle the rest." subtitle="Complete the form and send it directly through WhatsApp. A FixBar technician will contact you during business hours."/><section className="mx-auto max-w-7xl px-5 py-16 md:px-8 md:py-24"><div className="grid gap-10 lg:grid-cols-[1.4fr_1fr]"><div className="rounded-2xl border border-border bg-card p-6 md:p-8">{sent?<div className="flex min-h-96 flex-col items-center justify-center text-center"><CheckCircle2 className="size-16 text-primary"/><h2 className="mt-5 text-2xl font-extrabold">Your request is ready</h2><p className="mt-2 max-w-sm text-sm text-muted-foreground">WhatsApp has opened with your request details. Tap send to deliver it to the FixBar team.</p><a href={waUrl} target="_blank" rel="noopener noreferrer" className="mt-6 inline-flex items-center gap-2 rounded-full bg-primary px-6 py-3 text-sm font-bold text-primary-foreground"><MessageCircle className="size-4"/>Continue on WhatsApp</a><button type="button" onClick={()=>setSent(false)} className="mt-4 text-sm font-medium text-primary">Send another request</button></div>:<form onSubmit={submit} className="space-y-5"><div className="grid gap-5 sm:grid-cols-2"><Field id="name" label="Name" placeholder="Your full name"/><Field id="phone" label="Mobile number" placeholder="05xxxxxxxx" type="tel"/></div><div className="grid gap-5 sm:grid-cols-2"><Field id="cafe" label="Café name" placeholder="Café name"/><Field id="city" label="City" placeholder="Riyadh"/></div><div className="grid gap-5 sm:grid-cols-2"><Select id="device" label="Equipment type" options={["Espresso machine","Coffee grinder","Roaster","Automatic pour-over / V60","Other"]}/><Select id="service" label="Service type" options={["Emergency repair","Preventive maintenance","Spare parts","Plan inquiry"]}/></div><div><label htmlFor="details" className="mb-1.5 block text-sm font-medium">Problem or request details</label><textarea id="details" name="details" rows={4} placeholder="Describe what is happening with the equipment..." className={inputCls}/></div><button type="submit" className="flex w-full items-center justify-center gap-2 rounded-full bg-primary py-3.5 text-sm font-bold text-primary-foreground"><MessageCircle className="size-4"/>Send via WhatsApp</button></form>}</div><aside className="space-y-4"><div className="rounded-2xl border border-border bg-card p-6"><h2 className="text-lg font-bold">Direct contact</h2><ul className="mt-4 space-y-4 text-sm"><Info icon={Phone} label="Hotline" value={CONTACT.phoneDisplay}/><Info icon={Mail} label="Email" value={CONTACT.email}/><Info icon={MapPin} label="Coverage" value="Riyadh and all regions of Saudi Arabia"/></ul></div><div className="rounded-2xl border border-primary/25 bg-primary/5 p-6"><h2 className="text-lg font-bold text-primary">Emergency breakdown?</h2><p className="mt-2 text-sm leading-relaxed text-muted-foreground">Professional plan subscribers receive emergency response within 24 hours and temporary replacement equipment when required.</p></div></aside></div></section></PageShell>}
function Field({id,label,placeholder,type="text"}:{id:string;label:string;placeholder:string;type?:string}){return <div><label htmlFor={id} className="mb-1.5 block text-sm font-medium">{label}</label><input id={id} name={id} type={type} required placeholder={placeholder} className={inputCls}/></div>}
function Select({id,label,options}:{id:string;label:string;options:string[]}){return <div><label htmlFor={id} className="mb-1.5 block text-sm font-medium">{label}</label><select id={id} name={id} required defaultValue="" className={inputCls}><option value="" disabled>Select an option</option>{options.map(o=><option key={o}>{o}</option>)}</select></div>}
function Info({icon:Icon,label,value}:{icon:typeof Phone;label:string;value:string}){return <li className="flex items-center gap-3"><span className="flex size-10 shrink-0 items-center justify-center rounded-lg bg-primary/10 text-primary"><Icon className="size-4"/></span><div className="min-w-0"><p className="text-muted-foreground">{label}</p><p className="font-bold" dir="ltr">{value}</p></div></li>}
